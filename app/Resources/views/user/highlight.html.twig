<div class="row">
    {% for user in users %}
        <div class="col-sm-6 col-md-4">
            <div class="thumbnail">
                <div class="caption">
                    <h3>{{ user.username }}</h3>
                </div>
            </div>
        </div>
    {% endfor %}
</div>

{{ dump(users) }}
